- To activate venv, type "scripts\activate"
- To deactivate venv, just type "deactivate"


- To back a directory, type "cd.."

- Type in "set FLAST_APP=store.py" before typing in "flask run"
- In the shop directory, type "flask run" to run the flask application.
